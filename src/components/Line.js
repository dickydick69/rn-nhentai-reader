import React from 'react';
import {View} from 'react-native';

const Line = props => {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        borderBottomColor: '#505050',
        marginVertical: 10,
      }}
    />
  );
};

export default Line;
